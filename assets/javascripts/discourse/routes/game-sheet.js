import DiscourseRoute from "discourse/routes/discourse";

export default class GameSheetRoute extends DiscourseRoute {
  // Optionnel : on peut configurer le titre de la page ou d'autres comportements ici plus tard
}
